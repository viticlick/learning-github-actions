name: Prepare release

on:
  push:
    branches:
      - test

jobs:
  show-refs:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        name: checkout repository

      - name: get reference
        run: |
          git branch
          git show-branch -a
